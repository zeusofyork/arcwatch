FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /gpuwatch-agent ./cmd/gpuwatch-agent

FROM alpine:3.19
COPY --from=builder /gpuwatch-agent /usr/local/bin/gpuwatch-agent
ENTRYPOINT ["gpuwatch-agent"]
